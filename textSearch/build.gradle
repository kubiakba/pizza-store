dependencies {
    compile project (":common")
    compile "org.springframework:spring-context:${springContext}"
    compile "io.searchbox:jest:${jest}"
    compileOnly "org.projectlombok:lombok:${lombok}"
}
